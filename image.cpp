#include <iostream>
#include "image.hpp"
/*
Image::Image(const Image<T>& img)
:width(img.width), height(img.height), array(img.array){}
*/
/*
void Image::clear(T color){
	for(int i=0;i<width*height;++i)
		array[i] = color;
}
*/
/*
template <typename T>
void Image<T>::skip_line(std::istream & file){
char reading;
file.get(reading);
while(reading != '\n')
	file.get(reading);
}

template <typename T>
void Image<T>::skip_comments(std::istream & file){
char reading;
file.get(reading);

while(reading == '#'){
	skip_line(file);
	file.get(reading);
}

file.putback(reading);
}*/
